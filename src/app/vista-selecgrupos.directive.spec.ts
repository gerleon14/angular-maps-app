import { VistaSelecgruposDirective } from './vista-selecgrupos.directive';

describe('VistaSelecgruposDirective', () => {
  it('should create an instance', () => {
    const directive = new VistaSelecgruposDirective();
    expect(directive).toBeTruthy();
  });
});
