import { VistaEncargoDirective } from './vista-encargo.directive';

describe('VistaEncargoDirective', () => {
  it('should create an instance', () => {
    const directive = new VistaEncargoDirective();
    expect(directive).toBeTruthy();
  });
});
